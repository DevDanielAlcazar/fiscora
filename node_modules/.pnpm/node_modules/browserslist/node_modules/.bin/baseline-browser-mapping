#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../baseline-browser-mapping/dist/cli.cjs" "$@"
else
  exec node  "$basedir/../../../baseline-browser-mapping/dist/cli.cjs" "$@"
fi
# cmd-shim-target=C:/Users/GLOUU/Desktop/DESARROLLOS/2026/contable/node_modules/.pnpm/browserslist@4.28.2/node_modules/baseline-browser-mapping/dist/cli.cjs
