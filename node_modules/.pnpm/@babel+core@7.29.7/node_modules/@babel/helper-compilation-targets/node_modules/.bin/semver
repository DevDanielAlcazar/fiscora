#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../semver/bin/semver.js" "$@"
else
  exec node  "$basedir/../../../../semver/bin/semver.js" "$@"
fi
# cmd-shim-target=C:/Users/GLOUU/Desktop/DESARROLLOS/2026/contable/node_modules/.pnpm/@babel+helper-compilation-targets@7.29.7/node_modules/semver/bin/semver.js
